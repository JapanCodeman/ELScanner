Get RegisterNewBook component up and running online as soon as possible
  to start populating database

Change Await component to AdminAwait and all references

Change app.js from class to functional component? (leaning towards class)

Setup authorized routing (research best practices on JWT storage/usage)

Look for good graphing module to keep track of class/student words read

Move authentication to app.js and pass props down? Or use a callback from login.js to change state of app.js? map user to state in app.js?

Write routes for:
  updating student's word count

bugs:
  logged in as user (student) - navigate away from page to get 404 and 
  can no longer see logout button in header

  Cannot refresh PAGE!!!! <-- Annoying - how to fix using flask catchall?